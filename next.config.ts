// next.config.js
import { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["ecommerce.routemisr.com"], // غيره لو الصور من دومين آخر
  },
};

export default nextConfig;
